# Minimum spanning tree (MST)
## Template
```rust,editable,ignore
{{#include ../../code/src/graph/mst.rs:main}}
```

## Usage
```rust,editable,ignore
let graph: Vec<Vec<(usize, i64)>>;
let mst: Vec<Edge> = kruskal(&graph);
```
